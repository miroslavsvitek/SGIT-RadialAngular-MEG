# Release v1.1.0

Public release candidate accompanying manuscript v6.

## Changes relative to v1.0
- updates manuscript snapshot to v6;
- adds public-facing analysis filenames without changing archived computational logic;
- adds GitHub/Zenodo metadata (`CITATION.cff`, `.zenodo.json`);
- adds clinical-neurophysiology interpretation while preserving manuscript guardrails;
- documents exact release and submission workflow.

## Reproducibility status
Run `python verify_manifest.py`, `python verify_archive.py`, and `python reproduce_article.py` after extraction.
